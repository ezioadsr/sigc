export default {
  props: {
    label: String,
    icon: String,
    helper: String,
    errorLabel: String,
    count: [Number, Boolean],
    inset: String,
    dark: Boolean,
    labelWidth: Number
  }
}
